import styled from "styled-components";

export const Item = styled.li`
display: flex;
gap: 8px;
margin-bottom: 16px;

p {
    font-weight: 600;
}

button {
    margin-left: 12px;
    padding: 0px;

    width: 20px;
    height: 20px;

    border-radius: 10px;

    align-items: center;
}
`